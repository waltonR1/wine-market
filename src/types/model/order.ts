export interface OrderConfirmItem {
    id: number
    name: string
    spec: string
    price: number
    count: number
    image: string
}

export interface AddressInfo {
    name: string
    phone: string
    detail: string
}